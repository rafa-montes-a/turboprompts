<% if @current_user.id == 1 %>
  
  <div class="card mb-3">
    <div class="card-body">

    <h5>Admin settings</h5>

    <% @list_of_users.each do |a_user| %>

      <div class="row">

        <div class="col-auto">
          <%= a_user.id %>
        </div>
        
        <div class="col-auto">
          <%= a_user.first_name %> <%= a_user.last_name %>
        </div>

        <div class="col-auto">
          <%= a_user.email %>
        </div>

        <div class="col-auto">
          Created: <%= a_user.created_at.strftime(("%B %d, %Y")) %>,
        </div>

        <div class="col-auto">
          <%= a_user.prompts.count %> prompts, 
        </div>

        <div class="col-auto">
          <%= a_user.questions.count %> questions asked, 
        </div>

        <div class="col-auto">
          <%= a_user.likes.count %> likes given
        </div>

      </div>

    <% end %>

    </div>
  </div>


<% else %>
   <p class="text-center">What yu doin'?</p>
  <img class ="img-fluid" src="https://i.ibb.co/TrbgfGr/what.jpg"></img><br>
<% end %>
